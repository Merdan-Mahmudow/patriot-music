export interface Track {
    id: number
    artist: string
    url: string
    duration: string
    image: string
    name: string
}

export interface VideoType {
    id: number
    url: string
}

export interface ContactFormType {
    email: string
    phone: string
    telegram: string
    name: string
}